# Database URL (Railway PostgreSQL)
# Will be automatically linked from Railway PostgreSQL service
DATABASE_URL=postgresql://postgres:PASSWORD@host:port/railway

# Server port (optional, default 8000)
PORT=8000
